# [all:children]
# aws

# [aws]
# 100.29.250.35

# [aws:vars]
# ansible_user=ubuntu
# ansible_ssh_private_key_file={{ key_pair_path }}

all:
  children:
    aws:
      hosts:
        100.29.250.35:
      vars:
        ansible_user: ubuntu
        ansible_ssh_private_key_file: /var/lib/jenkins/server1.pem
